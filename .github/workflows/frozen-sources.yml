name: Frozen sources

on:
  pull_request:
  push:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify frozen Extra sources
        run: scripts/frozen-sources-fingerprint.py
